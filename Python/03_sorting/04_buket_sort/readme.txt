buket sort is the mos efficeinet sorting algoritthm.
It can run in O(N) time commplexity.
The only issue is that it can be used only if the input has a constrain where it fits into a constrained range.

if you are sure that the input is bounded to a finate range, then it is the most efficient method to sort.

time complexity    O(N)
memory complexity  O(1)
non stable sorting algorithm
