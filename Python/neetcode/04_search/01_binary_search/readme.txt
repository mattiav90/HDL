
binary search

time complexity O(logN)
memory complexity O(1)


binary search is a very simple searching method that works only when the list is already ordered. 
has time complexity O(N) because it always splits the range in half. 
